# Backend
BACKEND_PORT=8001
CORS_ALLOW_ORIGINS=*

# Optional Qwen / DashScope API for image recognition and AI fallback routes.
# Leave empty to use the built-in local fallback.
QWEN_API_KEY=
QWEN_MODEL=qwen-plus
QWEN_VL_MODEL=qwen-vl-plus
