{
  "name": "zhida-digital-wardrobe",
  "version": "2.0.0",
  "description": "本地优先的数字衣橱、穿搭推荐和试衣间演示项目。",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "python -m uvicorn backend.main:app --reload --port 8001",
    "start": "python -m http.server 8000 -d public",
    "build": "node scripts/validate.js",
    "lint": "node scripts/validate.js",
    "typecheck": "node scripts/validate.js",
    "test": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
